from geometry import *  # import all names if they don't start with "_"

circle = circle_area(8)
print(f"{circle = }")

rectangle = rectangle_area(10, 12)
print(f"{rectangle = }")

square = square_area(7.9)
print(f"{square = }")
